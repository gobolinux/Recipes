diff -ru grub-2.12.orig/grub-core/osdep/linux/getroot.c grub-2.12/grub-core/osdep/linux/getroot.c
--- grub-2.12.orig/grub-core/osdep/linux/getroot.c	2025-04-03 14:05:32.056666592 +0000
+++ grub-2.12/grub-core/osdep/linux/getroot.c	2025-04-03 14:12:30.343333252 +0000
@@ -373,6 +373,19 @@
   return NULL;
 }
 
+void
+translate_from_gobolinux (char *device)
+{
+  const char *gobodev = "/System/Kernel/Devices";
+  char *sep = strstr(device, gobodev);
+  if (sep == device)
+  {
+    char copy[strlen(device)+1];
+    int n = sprintf(copy, "/dev%s", &device[strlen(gobodev)]);
+    copy[n] = '\0';
+    strcpy(device, copy);
+  }
+}
 
 char **
 grub_find_root_devices_from_mountinfo (const char *dir, char **relroot)
@@ -435,6 +448,7 @@
       if (sscanf (sep, "%s %s", entry.fstype, entry.device) != 2)
 	continue;
 
+      translate_from_gobolinux (entry.device);
       unescape (entry.device);
 
       /* Using the mount IDs, find out where this fits in the list of
Only in grub-2.12/grub-core/osdep/linux: getroot.c.orig
diff -ru grub-2.12.orig/grub-core/osdep/linux/ofpath.c grub-2.12/grub-core/osdep/linux/ofpath.c
--- grub-2.12.orig/grub-core/osdep/linux/ofpath.c	2025-04-03 14:05:32.056666592 +0000
+++ grub-2.12/grub-core/osdep/linux/ofpath.c	2025-04-03 14:12:46.556666582 +0000
@@ -176,7 +176,7 @@
 	  if (fd >= 0)
 	    close (fd);
 	  kill_trailing_dir(sysfs_path);
-	  if (!strcmp(sysfs_path, "/sys"))
+	  if (!strcmp(sysfs_path, "/System"))
 	    {
 	      grub_util_info (_("`obppath' not found in parent dirs of `%s',"
 				" no IEEE1275 name discovery"),
