configure_options=(
   "--with-shared"
   "--with-termlib"
   "--without-debug"
   "--without-ada"
   "--enable-rpath"
   "--enable-termcap"
   "--enable-symlinks"
   "--enable-overwrite"
#   "--enable-widec"
)
