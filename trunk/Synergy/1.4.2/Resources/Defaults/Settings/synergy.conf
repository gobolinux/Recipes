# This is the default server configuration generated by Compile. In order to 
# make Synergy actually useful, edit this file according to the instructions at
# http://synergy2.sourceforge.net/running.html
section: screens
   THISHOST:
   # someOtherComputer:
   # Add a line with the hostname for each of the client machines here
end

section: links
   THISHOST:
   # left = someOtherComputer
   # someOtherComputer:
   # right = THISHOST
end
