[Name] Redis
[Summary] Key-value database
[Description] Redis is a key-value database. It is similar to memcached but the dataset is not volatile, and values can be strings, exactly like in memcached, but also lists and sets with atomic operations to push/pop elements.
[License] BSD
[Homepage] http://code.google.com/p/redis/
